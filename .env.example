# Copy to .env.local and fill in from your Supabase project settings.
# Both values are safe to expose in the browser — row level security is what
# protects the data, not these keys.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
