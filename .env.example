# Copy to .env.local and fill in from your Supabase project settings.
# Both values are meant to be public — they ship in the browser bundle. What
# protects the data is row level security, not their secrecy. Never put the
# service role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
